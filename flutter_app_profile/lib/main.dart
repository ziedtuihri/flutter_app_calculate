import 'package:flutter/material.dart';
import 'package:flutter_app_profile/mycard.dart';
import 'package:flutter_app_profile/wonder.dart';

void main() {
  //runApp(MyCard());
  runApp(Wonder());
}

class MyProfile extends StatelessWidget {
  const MyProfile({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(

    );
  }
}

